# Site password — client-side gate for published sites.
# Preferred: SHA-256 hash (generate via Menu → "Site password…").
# NEXT_PUBLIC_APP_PASSWORD_HASH=

# Legacy plaintext fallback (used only when no hash is set above).
NEXT_PUBLIC_APP_PASSWORD=your-secret-password
