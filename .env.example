# Site unlock password (baked in at build time — not changeable in the app).
# For local dev: copy to .env.local and set your password.
# For production: set GitHub Actions secret APP_PASSWORD (see README).

NEXT_PUBLIC_APP_PASSWORD=your-secret-password

# Or use a hash instead of plaintext (optional):
# NEXT_PUBLIC_APP_PASSWORD_HASH=
